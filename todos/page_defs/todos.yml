todos:
  capabilities:
  - name: TodoPage
  screens:
    - name: index
      regions:
        - type: row
          size: fixed
          components:
          - custom.TodoForm
        - type: row
          scrollable: true
          components:
          - custom.TodoList
        - type: row
          size: fixed
          components:
          - custom.TodoManager
