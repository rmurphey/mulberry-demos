home_grid_tablet:
  type: node
  screens:
  - name: index
    regions:
    - type: row
      regions:

      - type: row
        className: top
        size: fixed
        components:
        - custom.HomeHeader

      - type: row
        className: home-nav-columns
        scrollable: true
        components:
          - BodyText
          - ChildNodeGrid

    - type: row
      size: fixed
      components:
      - AppNav
