home-grid-phone:
  type: node
  screens:
  - name: index
    regions:
    - className: top-container
      regions:

      - className: top home-header-container
        components:
        - custom.HomeHeader

      - className: home-nav-columns
        scrollable: true
        components:
          - BodyText
          - custom.PhotoViewTwo

    - className: app-nav-container
      components:
      - AppNav
